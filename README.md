1) Very simply clone repo into $HOME directory (~/).
2) In ascending order, run the scripts from "/personal/<your distro>/dev/setup/"
3) Add vimium-options.json's into chromium and zen-browser respectively

OMARCHY
if running on an system with a very outdated cpu like an HD 4000:
    <CTRL+ALT+F3>
    $ sudo pacman -S alacritty
    $ nvim .config/xdg-terminals.list
    3 # com.mitchellh.ghostty.desktop
    4 Alacritty.desktop
    <ALT+F1>
