#!/usr/bin/env bash

# Installs the packages I like to have in Arch.
# I made this so I don't need to find and manually install each.


sudo pacman -S pigz pbzip2  # For AUR config(faster go brrr)
sudo pacman -S nano vim git less
sudo pacman -S brightnessctl playerctl
sudo pacman -S chromium

